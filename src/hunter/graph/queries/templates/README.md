# Cypher query templates (vetted)

PART 1 uses an in-memory graph with JSONL export. Neo4j templates for future phases live here; bind parameters only from allowlisted tool paths—never from raw LLM strings.
